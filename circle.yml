#
# CircleCI Configuration
#

# Machine Settings

machine:
  node:
    version: 6.2

# Dependencies

dependencies:
  pre:
  override:
    - nvm use 6.2
    - npm install -g angular-cli
    - npm install

# Test Commands

test:
  override:
    - nvm use 6.2 && npm run test
    #- npm run e2e